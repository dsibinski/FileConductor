<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:myNamespace="clr-namespace:MyNamespace"
             x:Class="FileConductorUI.UI.EditWatcherTabView" 
             mc:Ignorable="d" >

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock ="Top" Orientation="Horizontal">
                <Label Content="Regex"></Label>
                <TextBox Width ="100" Text="{Binding Watcher.FileNameRegex}"></TextBox>
            </StackPanel>
            <StackPanel DockPanel.Dock ="Top" Orientation="Horizontal">
                <Label Content="Code"></Label>
                <TextBox Width ="100" Text="{Binding Watcher.Code}"></TextBox>
            </StackPanel>
            <StackPanel Margin="50,50,50,20"  Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button Margin ="10" Width="50" Content="Save" Command="{Binding SaveCommand}"></Button>
                <Button Margin ="10" Width="50" Content="Exit" Command="{Binding CloseCommand}"></Button>
            </StackPanel>
            
        </DockPanel>

    </Grid>
</UserControl>
