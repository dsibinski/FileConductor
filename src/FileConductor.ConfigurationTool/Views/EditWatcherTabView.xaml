<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
            xmlns:configurationTool="clr-namespace:ConfigurationTool"
            xmlns:controls="clr-namespace:ConfigurationTool.Controls"
            x:Class="ConfigurationTool.Views.EditWatcherTabView" 
             mc:Ignorable="d" >

    <Grid>
        <DockPanel>
            <StackPanel Margin="50,50,50,20"  Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button Margin ="10" Width="50" Content="Save" Command="{Binding SaveCommand}"></Button>
                <Button Margin ="10" Width="50" Content="Exit" Command="{Binding CloseCommand}"></Button>
            </StackPanel>
            <GroupBox x:Name="Watcher" Header="Watcher" Height="auto" VerticalAlignment="Top">
                <Grid>
                    <StackPanel HorizontalAlignment ="Left" Orientation="Vertical">
                        <controls:CommonField Text="Code" Value="{Binding Watcher.WatcherData.Code}"></controls:CommonField>
                        <controls:CommonField Text="Regex" Value="{Binding Watcher.WatcherData.FileNameRegex}"></controls:CommonField>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </DockPanel>
    </Grid>
</UserControl>
